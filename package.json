{
  "name": "parcel-netlify-multiple",
  "version": "1.0.0",
  "license": "MIT",
  "source": [
    "./src/project-1/index.html",
    "./src/project-2/index.html"
  ],
  "scripts": {
    "start": "parcel",
    "prebuild": "rm -rf ./dist",
    "build": "parcel build",
    "prebuild-netlify": "rm -rf ./dist",
    "build-netlify": "parcel build --public-url $URL"
  },
  "devDependencies": {
    "parcel": "^2.1.1",
    "prettier": "^2.5.1"
  },
  "dependencies": {
    "alpinejs": "^3.7.1",
    "lodash": "^4.17.21"
  }
}
